import styled from "styled-components";

import { v } from "../../styledComponents/variables";

export const SLayout = styled.div`
  display: felx;
`;

export const SMain = styled.main`
  padding: calc(${v.xxlSpacing}*2);
`;
